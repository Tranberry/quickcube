name: quickcube
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

platforms:
  windows:
  linux:
  macos:

executables:
  quickcube: simple_project_creator

environment:
  sdk: ^3.0.1

# Add regular dependencies here.
dependencies:
  path: ^1.8.3
  uuid: ^3.0.7
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
